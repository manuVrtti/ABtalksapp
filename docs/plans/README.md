# ABTalks plans

Each file here is a single, ready-to-execute implementation plan handed to Cursor. Naming: `NNN-short-name.md` (zero-padded, sequential).

Format and Cursor guardrails are defined in the repo-root `CLAUDE.md`. Read that before writing or executing a plan.
